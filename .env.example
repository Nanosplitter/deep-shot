# Root .env for Docker Compose
# Copy values from backend/.env or set them here

OPENAI_API_KEY=${OPENAI_API_KEY}
TUNNEL_TOKEN=${TUNNEL_TOKEN}

# These will be read from backend/.env if you source it, 
# or set them directly here for Docker Compose
